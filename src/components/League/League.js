import React from 'react';
import './League.css'
const League = (props) => {
    console.log(props);
    // const { dateFirstEvent, strCountry, strSport, strGender } = props.league;
    return (
        <div className="league-container">

            
        </div>
    );
};

export default League;